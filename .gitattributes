# Normalize line endings: LF in the repo and in every checkout, even on Windows.
# Shell scripts with CRLF break inside the Linux image ("bad interpreter").
* text=auto eol=lf

*.sh text eol=lf
Dockerfile* text eol=lf
*.png binary
*.jpg binary
